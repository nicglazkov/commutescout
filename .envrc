# Pin every gcloud/firebase invocation in this tree to the right project,
# regardless of what the shared global gcloud config currently points at.
# Env vars beat `gcloud config set project` from any other session.
export CLOUDSDK_CORE_PROJECT=ca-roads-mcp
export GOOGLE_CLOUD_PROJECT=ca-roads-mcp
